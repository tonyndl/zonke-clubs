import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    // marginBottom: 4,
  },
  errorText: {
    fontSize: 12,
    color: "#ff3b30",
    marginTop: -16,
    marginLeft: 4,
    marginBottom: 8,
  },
});
